#include <stdio.h>
#include "lcd.h"

/*int main() {
    stdio_init_all();

    // Initialize the LCD
    lcd_init();

    // Display a message
    lcd_print("Hello, Wokwi!");

    while (true) {
        // Keep the message displayed
        sleep_ms(1000);
    }

    return 0;
}*/
